# Image2 AI-only production batch prod_01 - Tony

Owner: Tony
Task range: `img2-0007` through `img2-0056` from `image2_ai_only_task_queue.csv`
Output directory: `/mnt/f/CodexWorkspace/assembly_line_optimize/image2_ai_only_1000_atom_combinations`

## Current accepted baseline

Corrected pilot is now `20/20` on disk, all `1448x1086` PNG. Continue from the same corrected requirements:

- Bottle material and pose must come from `/mnt/f/CodexWorkspace/assembly_line_optimize/generated_bottle_pose_collection`.
- Replace the black bottle proxy only. Preserve conveyor, camera, manuals, manual text/QR/logo, positions, rotations, scale, and overlaps.
- For `lying_bar`, generated bottle long axis, length, and visual bbox must align to `bottle_proxy_bbox_xyxy`.
- For `upright_dot`, generate a top-down/upright bottle footprint matched to the dot bbox; do not expand it into a lying bottle.
- If there is no bottle proxy, do not add a bottle.
- Bottle must stay fully on the green conveyor and above manuals.
- Final file must keep the exact `output_path` filename and be `1448x1086` PNG.

## Working protocol

Process one image at a time. After each image:

1. Save the PNG to the exact `output_path`.
2. Verify it opens and is `1448x1086`.
3. Visually check bottle size/length against the black proxy from the input image.
4. Continue immediately to the next task in the range.

Do not stop at a small sample. If blocked on a specific file, report the task id, reason, and continue with the next file unless the blocker affects the whole batch.

Reference QA sheet from pilot: `/mnt/f/CodexWorkspace/assembly_line_optimize/qa_reports/image2_corrected_pilot_20_proxy_vs_output.jpg`
